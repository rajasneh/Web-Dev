const sum=(a,b)=>a+b;
const mul=(a,b)=>a*b;
const pi=3.14;

let obj={          // it is a object so we add function or properties to it
    sum :sum,
    mul :mul,
    pi :pi,
};
module.exports=obj;

//second method to export

//exports.subtract=(a,b)=>a-b;